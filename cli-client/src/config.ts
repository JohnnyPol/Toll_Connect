export const CONFIG = {
    API_URL: "http://localhost:9115/api", // Base URL for the backend API
    TOKEN_FILE: "token.txt", // Location where the authentication token is stored
    REQUEST_TIMEOUT: 10000, // Default timeout for API requests in ms
};
